import type { NextPage } from "next";
import Button from "../react-ui/button";
import Input from "../react-ui/textfield";

const Home: NextPage = () => {
  return (
    <div
      css={{
        margin: 10,
      }}
    >
      <Button />
    </div>
  );
};

export default Home;
