import { useCallback } from "react";

interface ServerConfig {
  url: string;
}

const useFetch = (serverConfig: ServerConfig) => {
  //   const useCallback(() => {}, [serverConfig]);
};

export default useFetch;
